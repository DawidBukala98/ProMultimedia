import 'package:flutter/material.dart';

abstract class AppColor {
  static const backgroundCardColor = Color.fromRGBO(238, 238, 238, 1);
  static const lightGreenColor = const Color.fromARGB(255, 115, 235, 145);
  static const whiteColor = Colors.white;
  static const blackColor = Colors.black;
  static Color backgroundButtonColor =
      Color.fromRGBO(245, 245, 245, 1).withOpacity(0.3);

  static Color backgroundTextColor =
      const Color.fromRGBO(117, 117, 117, 1).withOpacity(0.2);
}
