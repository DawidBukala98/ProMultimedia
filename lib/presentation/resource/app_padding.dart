import 'package:flutter/material.dart';

abstract class AppPadding {
  static const smallPadding = EdgeInsets.all(6);
  static const mediumPadding = EdgeInsets.all(12);
  static const bigPadding = EdgeInsets.all(18);
}
